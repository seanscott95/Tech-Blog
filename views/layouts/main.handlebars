<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/3.0.1/handlebars.min.js"></script>    
</head>

<body class="bg-dark text-white">
    <header class="display-flex justify-space-between align-center text-center p-2">
        <h1 class="text-info">
            Tech Blog
        </h1>
        <nav>
            <a class="text-info mr-3" id="homepage" href="/">Homepage</a>
            <a class="text-info ml-3" id="dashboard" href="/dashboard">Dashboard</a>

            <!-- Only display the logout button if the user is logged in -->
            {{#if logged_in}}
            <button class="text-info btn btn-link mb-1" id="logout">Logout</button>
            {{else}}
            <a class="text-info" id="login" href="/login">Login</a>
            {{/if}}
        </nav>
    </header>
    <main class="container container-fluid mx-auto text-white">
        {{{ body }}}
    </main>
    <footer>

    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Only include the logout script if the user is logged in -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

</body>

</html>